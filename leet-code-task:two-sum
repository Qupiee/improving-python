class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        for i in range(len(nums)):
            for j in range(i):
                if target == nums[i] + nums[j]:
                    newNums = []
                    newNums.append(i)
                    newNums.append(j)
                    return newNums
